// Admin: Scores entry page

import React from 'react';

const ScoresAdminPage: React.FC = () => {
  // Implementation placeholder
  // Admin only: enter weekly scores per player, lock/unlock transfers
  return (
    <div>
      <h1>Manage Scores</h1>
      {/* Week selector */}
      {/* Score entry form per player */}
      {/* Lock/unlock transfers button */}
    </div>
  );
};

export default ScoresAdminPage;
