# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it is gitignored)
#
# STRATEGY: .env contains LOCAL development defaults (localhost services).
# Cloud credentials for production/staging go in the Netlify UI only.
# To temporarily use cloud services locally, create .env.local (higher priority).

# MongoDB (local dev: localhost, cloud: set in Netlify UI)
MONGODB_URI=mongodb://localhost:27017/bearwood-fantasy
MONGODB_DB_NAME=bearwood-fantasy

# JWT authentication (access tokens)
# Use a strong, random secret - at least 32 characters
JWT_SECRET=your-secure-secret-here-min-32-chars

# Redis (local dev: localhost, cloud: set in Netlify UI)
# Cloud format: redis://:password@host:port (or rediss:// for TLS)
REDIS_URL=redis://localhost:6379

# Redis key prefix (set automatically by the preview-isolation build plugin for deploy previews)
# Leave empty for local dev and production.
# REDIS_KEY_PREFIX=

# Optional: CORS allowed origins (comma-separated)
# Leave empty to allow all origins in development
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
